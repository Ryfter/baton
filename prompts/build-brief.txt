You are the implementer for {{task_title}} ({{issue_ref}}). Repo: {{repo_path}}. {{branch_instruction}} Work ONLY on that branch. Terms per docs/glossary.md.

READ FIRST (your requirements, in this order):
{{reading_list}}

IMPORTANT — reconcile any spec older than the current master against the code as it stands now; note deltas in your plan doc before writing code.

DELIVERABLE 1 — plan doc: author docs/superpowers/plans/{{plan_filename}} (your task breakdown + spec-vs-master deltas). Commit it first.

DELIVERABLE 2 — implementation:
{{scope_blocks}}

HARD HOUSE RULES: PowerShell 7; utf8NoBOM; 965-byte shell-arg ceiling (long strings via files); never name variables $args/$input/$event/$matches/$host/$pid; CLI errors via [Console]::Error.WriteLine + exit 2, hooks exit 0; ConvertTo-Json -InputObject @(...) for arrays; unary-comma only on direct-assignment returns; guard every divide; tests hermetic (never real ~/.baton, ~/.claude — $env:TEMP with try/finally); BOX-PRIVATE: seeds/docs/tests carry placeholders only, never real quota/roster/allowance data.

VERIFY (do not claim green without running): run every scripts/test-*.ps1 suite; report exact PASS/FAIL totals per suite. Commit incrementally with clear messages; push the branch when done.

REPORT: files touched, commits (SHAs), deltas found, exact test counts, open questions. Do NOT open a PR and do NOT merge — the conductor handles that.
