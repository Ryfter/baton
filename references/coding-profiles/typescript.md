# TypeScript coding profile (lean)

- Verify: project test script (`npm test` / `pnpm test`) scoped to touched files
- Match existing tsconfig, imports, and formatter
- No new dependencies without explicit task scope
- Prefer small SEARCH/REPLACE edits over file rewrites
