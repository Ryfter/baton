# Python coding profile (lean)

- Verify: `python -m pytest -q` (targeted paths when possible)
- Prefer Ox Alpha diff_apply for small edits; Codex/Grok for multi-file agentic
- Do not paste whole trees — Efficiency Officer selects passages
- Type hints on public APIs; match existing style in file
- No new dependencies without explicit task scope
