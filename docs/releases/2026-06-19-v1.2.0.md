# v1.2.0 — The Conductor + Memory Bridge release (stable)

**Tag:** `v1.2.0` · **Date:** 2026-06-19 · **Previous:** `v1.2.0-rc2` (2026-06-19), line opened from `v1.1.0` (2026-06-04)

The **stable v1.2.0**. Promotes the `v1.2.0-rc2` candidate to final and adds **Sprint 5 —
Memory Bridge** on top. Both test gates are green, there are **0 open issues**, and the
remaining roadmap items (below) are explicitly future work, not half-built. The rc line ends here.

---

## Theme

v1.1 stood up the Fleet Conductor evolution (capability routing + cost engine + legibility
dashboard). **v1.2 makes Baton economic, autonomous, and self-correcting**: a governed-fleet
pipeline (Triage → Usage → Projects → Research Gate) that classifies, gates, and routes work
cost-optimally; the **Conductor (`/baton:go`)** natural-language front door that plans then
executes a task DAG autonomously; and now the **Memory Bridge**, which closes the loop —
Baton remembers what was tried and warns before repeating a known-bad fix. North star
unchanged: **autonomy** (stop the per-step 1/2 prompts) + **legibility** (always show, in plain
English, what each agent is doing), with **cost-optimization** as the through-line.

## Highlights (the full v1.2 line)

- **The Conductor (`/baton:go`)** — the capstone front door: plan-then-execute over a task DAG,
  full-auto, interrupting only on a **budget cap** or a **destructive action**, with event +
  decision (autonomous-guess) ledgers + a plain-English report as the legibility net.
- **Memory Bridge (`/baton:remember` · `/baton:recall` · `/baton:memory-promote`)** — *new in
  the final* — a Projectmem-style, box-private **problem→attempt→outcome** memory queried before
  acting (memory-as-governance: don't repeat a known-bad fix), with a **discover→crystallize**
  ratchet that promotes recurring patterns into deterministic Grimdex rules.
- **Governed-fleet v2 pipeline (Sprints 1–4)** — Triage classifies, the Usage Governor gates,
  Projects sync writes board state, the Research Gate issues a build/adopt/adapt verdict — all on
  one cost-aware routing spine.
- **Models-as-tools registry** — `/baton:models` inventory engine, capability claims + context
  floors, economy/champion selection, gauntlet-scorecard import into the quality blend.
- **Cost-Optimization Engine Slices B + C** — the draft→finish cascade, then the cascade wired
  into the autonomous run-loop (concurrent driver, paid-peak gate, surge-scaled parallelism).

---

## What's new since v1.2.0-rc2

### Sprint 5 — Memory Bridge (merged `2cb3f45`, PR #60; decision d054)
A Projectmem-style dev memory. `memory-lib.ps1` appends **problem→attempt→outcome** rows to a
box-private `$BATON_HOME/memory-journal.jsonl`, then on the next task warns when the new work
matches a past attempt — especially *"you tried this fix before and it failed."*

- **Hybrid matching (d-mb-1):** a deterministic normalized **signature** (token-set key) drives
  the hermetic match — the warning you can trust; **`-Deep` semantic KB recall** is advisory
  *discovery* layered on top.
- **Discover→crystallize ratchet (d-mb-2):** semantic recall proposes patterns → the operator (or
  the auto-watcher) promotes → the deterministic key enforces the crystallized rule thereafter.
- **Two nomination paths, one visible write (d-mb-3):** `Get-PromotionCandidates` auto-**watches**
  (failed ≥ N → an *avoid* rule; winner seen ≥ N → a *prefer* rule), or the operator **flags**
  `<id|signature>`; both run the same `Invoke-MemoryPromote` → a visible Grimdex write → source
  rows stamped `promoted`. Nothing is promoted silently.
- **Pluggable seams (d-mb-4, d047 honored):** `Invoke-MemorySource` (`-Producer`) and
  `Invoke-MemoryPromote` (`-Writer`) — manual capture is v1's source and the box-private lessons
  file is v1's target; Conductor-ledger ingest and scope/kind-driven write routing plug in behind
  the seams later, no query-core change.

Surfaces: `/baton:remember`, `/baton:recall` (`--deep`, `--json`), `/baton:memory-promote`
(`[<id|signature>]`). Advisory, never blocking; box-private. **33-check hermetic suite** (a
`BATON_MEM_LESSONS` temp redirect keeps the promote test off the real KB) + 2 bootstrap deploy
asserts. Final Opus whole-branch review: **READY TO MERGE, 0 Critical / 0 Important.** Deployed
live + hermetic smoke green end-to-end.

### Promotion to stable
Plugin version finalized `1.2.0-rc.13 → 1.2.0`. No functional change from rc2 + Sprint 5; the rc
line is closed.

> The complete rc1→rc2 delta (the Conductor capstone, Sprints 1–4, models-as-tools, Engine Slices
> B+C — 111 commits / 167 files) is documented in
> [`2026-06-19-v1.2.0-rc2.md`](2026-06-19-v1.2.0-rc2.md). This note covers only the final's addition.

---

## Decisions captured

d037–d054 across the v1.2 arc (models-as-tools registry + claims, cascade modes,
triage/usage/projects/research-gate architectures, Conductor substrate + interrupts + hand-off,
and the Memory Bridge hybrid-matching + promotion ratchet). Recorded in the Grimdex KB under
`projects/baton/decisions/`.

## Test gate (green)

- **PowerShell suites** — all exit 0, including memory **33/33**, conductor **59/59**, research-gate
  **38**, triage, usage, projects, routing S1–S4, prime-hours, models, cascade, and **bootstrap
  39/39** (deploys cleanly + live smoke OK).
- Both the Conductor and the Memory Bridge were smoke-tested against their **deployed** scripts
  end-to-end (hermetic, zero-network).

## Upgrade notes

- Run `pwsh scripts\bootstrap.ps1 -Force` to deploy the new libs/commands/seeds (idempotent). New
  since rc2: `memory-lib.ps1` + `fleet-memory.ps1` and the `/baton:remember` · `/baton:recall` ·
  `/baton:memory-promote` commands.
- **No breaking changes.** The Memory Bridge is box-private and opt-in (the journal is created on
  first `remember`); all prior behavior is preserved by default.

## Not in this release (roadmap — explicitly future, not half-built)

- **Sprint 6 — Worker Adapter:** run one external worker through Baton routing; the sprint where
  `gh models run <model>` becomes a *budgeted* fleet worker (the GitHub allotment as a saturable
  labor pool). Then **Sprint 7 — Acceptance/Polish gates.**
- **Memory Bridge follow-ups:** scope/kind-driven write-target routing (universal target /
  `decisions-lib.ps1`); Conductor-ledger auto-ingest as a second `MemorySource`; `Set-MemoryPromoted`
  key-order; `recall --json` test coverage.
- **Conductor follow-ups:** precise plan-validity errors for duplicate / self-dependency task ids;
  direct `Invoke-Conductor` tests for the `failed` / `plan-invalid` paths; the Style-B standalone
  broker + web cockpit behind the file-based seam.
- Cost/speed advisor (two dials + infra inventory); overnight capability floor-finder (d036);
  router refinements (per-prompt similarity, blend-weight auto-tuning).
