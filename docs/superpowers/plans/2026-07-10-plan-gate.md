# Plan Gate — implementation plan

**Spec:** `docs/superpowers/specs/2026-07-10-plan-gate-design.md`  
**Decision:** Grimdex d080  
**Branch (suggested):** `feature/plan-gate`  
**Plugin bump:** after Slice 2 green (e.g. 1.11.x → next)

## How to run this plan

Build **subagent-driven** when possible; keep hermetic tests green per task. Do **not**
merge without Kevin's word. Reference d080 by id — do not re-copy the full decision into
the app repo.

### Already shipped (skip)

- [x] d080 in Grimdex
- [x] `GROK.md` + `.grok/rules/baton-handoff.md` + agent-handoffs registry
- [x] Grimdex wire targets include `GROK.md`

---

## Slice 0 — Fleet: Grok + `plan-review`

**Goal:** codex + grok-cli claim `plan-review`; Grok answers a canary.

### Task 0.1 — Seed + live fleet rows

**Files:**

- `references/fleet.yaml` — taxonomy comment + `grok-cli` row + explicit `plan-review`
  (and likely `review`) on `codex`; document capability in the header comment list.
- Live `~/.baton/fleet.yaml` — same grants (hand-edit; bootstrap will not re-seed).

**Canary:**

```powershell
grok -p "Reply with exactly: PONG"
# after row enabled:
pwsh -NoProfile -File scripts/fleet-doctor.ps1   # PATH reachability
# if --live supported on this box:
pwsh -NoProfile -File scripts/fleet-doctor.ps1 -Live  # or documented flag
```

**Done when:** `grok-cli` listed enabled; canary PONG; doctor does not mark it missing.

### Task 0.2 — stdin vs inline lock

Mirror v1.11.1 agy/codex lessons. Prefer stdin for JSON-heavy plan-review prompts if
Grok accepts it; else `stdin: false` + file-based prompts (`--prompt-file`) for the
dispatcher path.

**Done when:** one long-prompt / quote-heavy canary does not no-op or hang.

---

## Slice 1 — Standalone Plan Gate

**Goal:** `/baton:plan-gate` works without Conductor changes.

### Task 1.1 — `plan-gate-lib.ps1` pure + seamed

**Files:** `scripts/plan-gate-lib.ps1` (new)

Reuse `gate-lib.ps1` findings helpers where possible. Implement
`Get-PlanReviewVerdict`, `Format-ReviseBrief`, `Format-PlanGateReport`,
`Build-PlanReviewPrompt`, `Invoke-PlanGate` per spec §4.2.

### Task 1.2 — Hermetic suite

**Files:** `scripts/test-plan-gate-lib.ps1` (new)

Cases from spec §5 (no network). Exit 0.

### Task 1.3 — CLI + command + bootstrap

**Files:**

- `scripts/fleet-plan-gate.ps1`
- `commands/plan-gate.md`
- bootstrap deploy list + `scripts/test-bootstrap.ps1` asserts

**Done when:** suite green; bootstrap green; dry CLI with stub path documented.

---

## Slice 2 — Conductor wiring

**Goal:** `/baton:go --plan-gate` once-overs the plan before the DAG walk.

### Task 2.1 — `Invoke-Conductor` plan-gate phase

**Files:** `scripts/conductor-lib.ps1`, `scripts/fleet-go.ps1`, `commands/go.md`

- Opt-in flags; default path unchanged.
- Write `plan-review.json`; `plan-rejected` status; one revise pass.

### Task 2.2 — Conductor tests

**Files:** `scripts/test-conductor-lib.ps1` (and go-execute if needed)

No-flag unchanged; reject stop; revise once; understaffed fail-open.

---

## Slice 3 — Polish / defaults (after live smoke)

- [ ] Default `--plan-gate` on for `-Execute` only after live codex+grok once-over proven
- [ ] Coach footer when last run was `plan-rejected` / revise
- [ ] Optional: `docs/COMMANDS.md` entry
- [ ] Plugin version bump + release notes + bootstrap redeploy

---

## Acceptance checklist (whole feature)

1. `pwsh -File scripts/test-plan-gate-lib.ps1` green  
2. Conductor / bootstrap suites still green  
3. Live: plan-gate with real codex + grok-cli on a sample `plan.json` produces
   `plan-review.json` with parsed findings or clean accept  
4. Live: `/baton:go --plan-gate` (or `-PlanGate`) on a tiny goal either accepts or
   revises once; reject path exercised with a stub if needed  
5. Kevin merge word before master  

## Suggested Claude opener

> Read `docs/next-session.md` (top resume), then  
> `docs/superpowers/specs/2026-07-10-plan-gate-design.md` and  
> `docs/superpowers/plans/2026-07-10-plan-gate.md`.  
> Grimdex decision **d080**. Build Slice 0 → 1 first; stop for review before Slice 2.  
> Do not re-author GROK.md / wire-project (already shipped).
